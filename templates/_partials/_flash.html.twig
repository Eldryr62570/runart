
{% for message in app.flashes('success') %}
    <div class="success flex flex-row justify-center items-center mb-4 bg-lime-300 rounded text-green-900 border border-green-900 p-3">
        {{ message }}
    </div>
{% endfor %}